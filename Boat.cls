// Classe que representa um barco
public class Boat {
    // Propriedade que armazena o modelo do barco
    public String model;

    // Construtor da classe que inicializa o modelo do barco
    public Boat(String model) {
        // Atribui o valor do parâmetro 'model' à propriedade 'this.model' do objeto
        this.model = model;
    }
}